"""
Only metrics:

global coverage
bin coverage
average interval length
maybe width by label and by bin

This would separate because it depends on model/features, and I don't want to contaminate the basic binning with TensorFlow or embeddings.
"""