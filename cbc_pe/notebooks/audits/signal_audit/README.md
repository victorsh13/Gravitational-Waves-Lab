# Signal audit scripts

This directory contains exploratory scripts and notebooks used to inspect generated 32-second signal datasets.

These files are not automated tests.

They may contain local dataset paths and should be treated as manual audit utilities.

Automated tests should live under `cbc_pe/tests/` and should avoid hardcoded user-specific paths.
