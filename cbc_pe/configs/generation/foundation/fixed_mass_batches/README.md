# Fixed-mass foundation batches

This directory contains generated configuration files for fixed-mass, GW-only 32-second signal batches.

Each JSON file generates one HDF5 file with 100 samples for a specific mass pair and seed/output-file index.

These configs were created for supervisor-requested foundation-model tests where multiple files per mass pair were needed.

The files are reproducible generation configs, not temporary scratch files.
