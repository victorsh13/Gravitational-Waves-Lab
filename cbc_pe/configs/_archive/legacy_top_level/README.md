# Legacy top-level configs

This directory contains old top-level configuration files kept for historical reference.

The active configuration layout is:

- `configs/generation/`
- `configs/splits/`
- `configs/experiments/`

New runs should not use the JSON files in this archive.
